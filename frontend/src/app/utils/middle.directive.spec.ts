import { MiddleDirective } from './middle.directive';

describe('MiddleDirective', () => {
  it('should create an instance', () => {
    const directive = new MiddleDirective();
    expect(directive).toBeTruthy();
  });
});
