name: Test action
run-name: ${{ github.actor }} is testing 
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from ${{ github.event_name }} event."
